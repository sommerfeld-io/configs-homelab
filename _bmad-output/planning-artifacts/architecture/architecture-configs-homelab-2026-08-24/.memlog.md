---
scope: Whole Homelab Configs system (brownfield, initiative altitude)
purpose: build-substrate, spine only
altitude: initiative
updated: 2026-08-24T22:45
---

- (decision) Intent: Create. Input: finalized PRD at _bmad-output/planning-artifacts/prds/prd-configs-homelab-2026-08-24/prd.md (whole-system scope) -- altitude set to initiative
- (decision) Working mode: Coaching path (user choice). Purpose: spine only, no additional human-facing artifact requested
- (event) Brownfield: dispatching code-sweep subagent to ground coaching conversation in existing conventions before eliciting decisions
- (decision) [ADOPTED] Paradigm: declarative-convergence via Ansible. Ansible is the norm for anything repeatable/idempotent; imperative one-off scripts are permitted as an accepted exception (not banned), but never for anything meant to run again or apply to more than one node
- (decision) [ADOPTED] No node-specific state lives outside the Ansible declaration -- the declaration (playbooks/roles/vars/inventory, vault-encrypted where secret) is the single source of truth, with no separate state ledger to reconcile against. This is explicitly why Terraform was never adopted for this project: Terraform's separate state-file model conflicts with this invariant (user: 'this is the reason i never used terraform')
- (event) Code sweep complete (subagent). Ratifying consistently-followed conventions silently: (1) role boundary heuristic -- generic/reusable/OS-level tooling lives in the ansible-roles-collection submodule, homelab-specific single-purpose deployments live in local ansible/roles/{common,grafana-cloud,media,security}; (2) task naming convention 'Category ---- Subcategory ---- Action' (double-space + 4-dash) used consistently across playbooks/roles; (3) task-runner add-new-playbook pattern via shared YAML anchors (&ansible-desc/&ansible-cmd) in ansible/taskfile.yml, same pattern for vault tasks; (4) vault vars referenced directly by name in tasks, no vault_ prefix indirection, task ansible:vault(:grafana) is the only sanctioned edit path; (5) CI division of labor -- this repo's CI is lint + InSpec-profile-vendor-validity + docs + release only, never executes playbooks against a live/containerized target; multi-Ubuntu-version role testing lives only in the roles-collection submodule's own CI (matches PRD FR-5)
- (question) Per-machine exceptions: THREE distinct mechanisms found in live use (host-scoped extra play appended inside the group playbook e.g. raspi.yml pi4-01/02 disk mounts; a wholly separate single-host playbook imported from the group playbook e.g. desktop-media.yml/caprica; inline inventory host-vars e.g. hosts.yml ollama_models). PRD FR-2 undersells this as one mechanism. Need user call: ratify all three (name when each applies) or converge to one
- (question) common/taskfile-dev local role duplicates the submodule's ansible-roles-collection/taskfile-dev role, both included back-to-back in desktop/server/raspi.yml with no documented rationale -- is 'local role with same name as a collection role' an intentional override/extension pattern worth codifying, or accidental duplication to clean up?
- (question) desktop-media.yml is imported from desktop.yml (hosts: ubuntu_desktop) but is itself scoped to caprica.fritz.box, which is actually in the ubuntu_server group per hosts.yml -- likely dead import or a host-group boundary looser than documented. Flag to user
- (question) PRD FR-7 ('every top-level ansible/ directory has a corresponding docs/ directory') overclaims reality: only ansible/playbooks/ has a complete 1:1 docs mirror; docs/ansible/roles/ covers 1 of ~29 roles (hardcoded copy in task docs:generate); ansible/tasks/ and ansible/vars/ have no docs counterpart at all. Spine needs the real rule; PRD may need a follow-up correction
- (assumption) ansible/roles/security/crowdsec-lynis is an empty, unreferenced, apparently-untracked role stub -- treating as a parked/abandoned placeholder outside this spine's scope unless user says otherwise
- (assumption) Most provisioning playbooks (all/desktop/server/raspi/ollama/grafana-agents) are excluded from ansible-lint due to vault-file references, tracked as existing GitHub issue #172 -- treating as Deferred (pre-existing tracked tech debt, not a new architectural decision for this spine) unless user wants it elevated
- (decision) [ADOPTED] Manual node drift: LOOSER policy. Transient manual poking/debugging directly on a node is fine; only changes meant to persist must make it back into the Ansible declaration. The system does not promise to catch you mid-experiment. No workstation-vs-headless distinction made -- applies uniformly
- (decision) [ADOPTED] Per-machine exception mechanisms: all three in live use are ratified, each mapped to its trigger: (a) host-scoped extra play appended inside the group playbook -- for a per-host PHYSICAL/hardware attachment unique to that machine (e.g. raspi.yml pi4-01/02 disk mount, since a USB HDD is physically connected to only one Pi); (b) separate single-host playbook, targeted/imported independent of formal group membership -- for a specific SERVICE/capability not every node of that role needs (e.g. desktop-media.yml -> caprica.fritz.box, deliberately bypassing caprica's ubuntu_server group membership: not every node needs every service even though all nodes of a role share the same foundation); (c) inline inventory host-vars on a capability group cutting across node-role groups (e.g. ollama_models on the ollama group). User confirmed (b) is intentional, not a bug
- (decision) [ADOPTED] Local role sharing a collection role's name (e.g. common/taskfile-dev vs ansible-roles-collection/taskfile-dev) is an intentional layering convention, not accidental duplication: the submodule role installs/provides the generic, reusable mechanism (software); the local same-named role supplies homelab-specific concrete content on top (concrete taskfiles/config). This generalizes the role-boundary heuristic already ratified from the code sweep: submodule = generic mechanism, local = concrete homelab-specific content -- and the pattern holds even when both share a role name, always included submodule-role-then-local-role in that order
- (decision) [ADOPTED] Docs-mirrors-ansible rule narrowed from PRD FR-7's overclaim: only ansible/playbooks/ requires a 1:1 docs/ counterpart (one .md per playbook). ansible/roles/, ansible/tasks/, ansible/vars/ do NOT require a docs/ mirror. Follow-up: PRD FR-7 text should be corrected in a later PRD update to state this narrower rule instead of 'every top-level ansible/ directory'
- (event) Removed ansible/roles/security/ (empty, unreferenced, untracked crowdsec-lynis role stub) per user instruction -- confirmed via git ls-files it held zero tracked files before deletion, no history lost
- (decision) [ADOPTED] Dependency pinning: dev-sec/linux-baseline pinned by tag (reproducibility) is the default rule for third-party InSpec deps. sommerfeld-io/inspec-profiles is an intentional exception, floating on branch:main, since the user controls both repos and wants it to always track latest. All 4 inspec.yml files share one version (0.92.1), bumped together as a set
- (decision) [ADOPTED] Single environment: the live homelab itself, no separate dev/staging tier. External providers: Grafana Cloud (observability) and GitHub (repo hosting + Actions CI + release). User confirmed nothing else to add -- coaching complete, moving to distill
- (event) Distilled ARCHITECTURE-SPINE.md from memlog: 9 ADs (AD-1..AD-9, all ADOPTED), 3 consistency-convention rows, Stack table, Structural Seed (source tree + 2 mermaid diagrams), Capability->Architecture Map for FR-1..FR-9, 6 Deferred items including the PRD FR-7 correction follow-up
- (decision) [ADOPTED] AD-4 test refined per user: role placement is decided by 'would this be useful to someone else / another project', not by mechanism genericity. common/mount-disk correctly stays local under this test even though its code is fully generic/parameter-driven, because mounting an arbitrary disk only matters due to this homelab's specific USB-drive setup
- (event) Reviewer gate complete (lint_spine.py clean; rubric walker 2 high/1 med/3 low/1 info; PRD reconciliation 2 medium gaps; version-verify 0 red flags but 1 date correction; adversarial 14 findings, min 10 required). Applied fixes: AD-2 (added FR-8 doc-tagging obligation + enforcement-gap note), AD-3 (explicit carve-out excluding AD-2 permanent exceptions), AD-4 (refined test per user answer + ordering-enforcement-gap note), AD-5 (tiebreakers for a/b co-occurrence, group-wide targeting for opt-in b-style playbooks, host-vars criterion for c vs inline union), AD-6 (narrowed Binds to node-config secrets, carved out CI/GitHub-Actions secrets), AD-7 (broadened Binds to CI tool images, clarified ansible-roles-collection floats via git submodule update --remote, noted docker-compose.yml pin/float inconsistency), AD-8 (syntax-check/dry-run carve-out vs AD-8's prohibition on applying playbooks in CI), AD-9 (acknowledged existing grafana-cloud/alloy role-doc exception, added rename/delete rule), Stack table (submodule entry corrected from false pin to floating), deployment diagram (fixed GitHub->node edges that contradicted AD-8, now shows Operator as the provisioning actor), Pi-node label (spelled out exact numbers instead of misleading range), Structural Seed (added VM inventory-placement note), Deferred (corrected InSpec 5.x EOL date Apr2026->Aug2027, flagged issue #172 as unresolvable/stale, added AD-2/AD-4 enforcement gaps and AD-7 docker-compose.yml inconsistency as concrete Deferred items, consolidated the two PRD follow-up corrections)
- (event) lint_spine.py re-run after all fixes: 0 findings, clean
- (event) Triage: no [ASSUMPTION] tags or open questions remained in the spine -- all resolved via coaching answers directly. No additional human-facing artifact requested (spine only), so doc_standards polish and Renderings step skipped. No external_handoffs configured. spine finalized
