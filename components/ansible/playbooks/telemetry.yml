---
- name: Common telemetry setup und services for all nodes
  hosts: ubuntu_desktop:raspi
  gather_facts: true
  become: false
  vars_files:
    - ../vars/global.yml
  tasks:

    - ansible.builtin.import_tasks: ../tasks/common-telemetry.yml

- name: Run monitoring stack (not the metrics) on dedicated host(s)
  hosts: telemetry.fritz.box
  gather_facts: true
  become: false
  vars_files:
    - ../vars/global.yml
  tasks:
