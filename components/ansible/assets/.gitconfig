[alias]
        all = "!f() { ls | xargs -I{} git -C {} $1; }; f"
[user]
        email = sebastian@sommerfeld.io
        name = sebastian
[credential]
        helper = cache --timeout=3600
[pull]
        rebase = false
[safe]
        directory = /home/sebastian/work/repos/sommerfeld-io/configs-homelab
