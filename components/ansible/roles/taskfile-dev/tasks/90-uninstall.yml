---
- name: Task  ----  Uninstall  ----  Remove directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  with_items:
    - "/home/{{ ansible_user }}/.docker-stacks/ops"
    - "/home/{{ ansible_user }}/.docker-stacks/portainer/assets"
