---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: dashboard-node-exporter
  namespace: telemetry
spec:
  json:
    configMapRef:
      name: grafana-dashboards
      key: grafana/dashboards/10-node-exporter-full.json
  # datasourceSelector:
  #   matchLabels:
  #     grafana_datasource: "true"
