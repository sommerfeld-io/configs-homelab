---
machine:
  network:
    hostname: talos-cp
  install:
    disk: /dev/mmcblk0
  certSANs:
    - talos-cp.fritz.box
  nodeLabels:
    node.network-connect: switch
    node.device: raspi-4
    node.storage: 32gb
    node.type: controlplane
