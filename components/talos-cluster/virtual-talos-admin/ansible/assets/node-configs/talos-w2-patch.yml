---
machine:
  network:
    hostname: talos-w2
  install:
    disk: /dev/mmcblk0
  certSANs:
    - talos-w2.fritz.box
  nodeLabels:
    node.network-connect: switch
    node.device: raspi-4
    node.storage: 32gb
    node.type: worker
