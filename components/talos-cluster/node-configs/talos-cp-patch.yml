---
machine:
  network:
    hostname: talos-cp
  install:
    disk: /dev/mmcblk0
  certSANs:
    - talos-cp.fritz.box
  nodeLabels:
    node.arch: arm64
    node.connected-to: switch
    node.device: raspi-4
    node.storage: 32gb
    node.type: controlplane
