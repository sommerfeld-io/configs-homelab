---
machine:
  network:
    hostname: talos-w2
  install:
    disk: /dev/mmcblk0
  certSANs:
    - talos-w2.fritz.box
  nodeLabels:
    node.arch: arm64
    node.connected-to: switch
    node.device: raspi-5
    node.storage: 128gb
    node.type: worker
