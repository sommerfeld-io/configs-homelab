---
machine:
  network:
    hostname: talos-w2
  install:
    disk: /dev/mmcblk0
  certSANs:
    - talos-w2.fritz.box
  nodeLabels:
    node.network-connect: switch
    node.device: raspi-5
    node.storage: 128gb
    node.type: worker
