Tagged on {{moment-format date "YYYY-MM-DD"}} | Compare v{{previousTag}}...v{{currentTag}}

{{#if commits}}
  {{#each commits}}
    {{#if this.notes}}
      {{#each this.notes}}
        {{#if this.title}}
          ### {{this.title}}
        {{/if}}
        {{#each this.commits}}
          * {{this.message}} ({{this.hash}})
        {{/each}}
      {{/each}}
    {{/if}}
  {{/each}}
{{/if}}
