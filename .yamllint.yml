---
extends: default

# Helm charts are excluded because they are validated with helm lint
ignore: |
  node_modules
  .vagrant
  target
  tests/inspec/*/vendor
  ansible/roles/ansible-roles-collection
  ansible/vars/vault.yml
  ansible/vars/grafana-vault.yml
  ansible/host_vars/*/*-vault.yml

rules:
  line-length:
    max: 150
    allow-non-breakable-inline-mappings: true
    level: warning
  new-lines:
    type: unix
  braces:
    max-spaces-inside: 1
    level: error
  brackets:
    max-spaces-inside: 1
    level: error
  comments:
    min-spaces-from-content: 1
  comments-indentation: false
  truthy:
    check-keys: false
  octal-values:
    forbid-implicit-octal: true
    forbid-explicit-octal: true
